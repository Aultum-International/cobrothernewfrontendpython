/**
 * Global currency hook — re-exports CurrencyProvider context.
 * Use selectedCurrency / setCurrency / formatPrice() for display + checkout currency.
 */
export { useCurrency as default, useCurrency } from '../context/CurrencyContext';
